/**
 * @file Color Palettes
 * @module config/color-palettes
 * @author Lexus Drumgold <lex@flexdevelopment.llc>
 */

/**
 * Primary color palettes.
 * Each key represents the id of a theme.
 *
 * @type {Object}
 */
const PRIMARY = {
  /**
   * Primary color palette for `default` theme.
   *
   * @property {string} default
   */
  default: {}
}

/**
 * Secondary color palettes.
 * Each key represents the id of a theme.
 *
 * @type {Object}
 */
const SECONDARY = {
  /**
   * Secondary color palette for `default` theme.
   *
   * @property {string} default
   */
  default: {}
}

/**
 * Alert color palettes.
 * Each key represents the id of a theme.
 *
 * @type {Object}
 */
const ALERT = {
  /**
   * Alert color palette for `default` theme.
   *
   * @property {string} default
   */
  default: {}
}

export { PRIMARY, SECONDARY, ALERT }
